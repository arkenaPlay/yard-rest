<% unless @meths.empty? %>
  <div id="Services">
    <div class="api_actions_header">Actions</div>
    <% @meths.each_with_index do |meth, i| %>
      <%= yieldall :object => meth, :index => i %>
    <% end %>
  </div>
<% end %>
